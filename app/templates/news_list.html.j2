<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>News List</title>
</head>
<body>
    <h1>News List</h1>
    <ul>
        {% for news_item in news %}
        <li><a href="{{ url_for('news_detail', id=news_item.id) }}">{{ news_item.title }}</a></li>
        {% endfor %}
    </ul>
</body>
</html>