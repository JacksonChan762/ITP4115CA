<!DOCTYPE html>
<html>
<head>
    <title>News List</title>
</head>
<body>
    <h1>News List</h1>
    <h2>All News Items</h2>
<ul>
    {% for news_item in news %}
    <li>
        <a href="{{ url_for('news_detail', news_id=news_item.id) }}">{{ news_item.title }}</a> - 
        {{ news_item.content | truncate(100, True, '...') }} <br>
        <strong>Author:</strong> {{ news_item.author.name if news_item.author else 'Unknown' }}
    </li>
    {% endfor %}
</ul>

</body>
</html>
