<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>News Detail</title>
</head>
<body>
    <h1>{{ new.title }}</h1>
    <img src="{{ url_for('static', filename=new.image_filename) }}" alt="News Image">
    <p>{{ new.content }}</p>
    <p>Published on: {{ new.addtime }}</p>
    <a href="{{ url_for('news_list') }}">Back to News List</a>
</body>
</html>