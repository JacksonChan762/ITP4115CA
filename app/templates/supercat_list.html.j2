<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>主分類列表</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <h1>主分類列表</h1>
        <div class="mb-3">
            <a href="{{ url_for('supercat_add') }}" class="btn btn-primary">新增主分類</a>
        </div>
        <table class="table">
            <thead>
                <tr>
                    <th>編號</th>
                    <th>分類名稱</th>
                    <th>添加時間</th>
                    <th>操作</th>
                </tr>
            </thead>
            <tbody>
                    {% for supercat in supercats %}
                    <tr>
                        <td>{{ supercat.id }}</td>
                        <td>{{ supercat.cat_name }}</td>
                        <td>{{ supercat.add_time }}</td>
                        <td>
                            <form action="{{ url_for('supercat_del') }}" method="post">
                                <input type="hidden" name="id" value="{{ supercat.id }}">
                                <input type="submit" value="Delete">
                            </form>
                        </td>
                    </tr>
                    {% endfor %}
            </tbody>
        </table>
    </div>
</body>
</html>